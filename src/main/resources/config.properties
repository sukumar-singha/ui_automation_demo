# Browser Configuration
browser=chrome
headless=false

# Application URLs
base.url=https://example.com
login.path=/login

# Timeouts (in seconds)
timeout.implicit=10
timeout.explicit=20
timeout.pageload=30

# Test Data
test.username=user
test.password=password123